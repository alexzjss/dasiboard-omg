{
  "name": "dasiboard-ts",
  "version": "2.0.0",
  "description": "DaSIboard — Dashboard para estudantes de SI-USP. Reescrito em TypeScript.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit && vite build",
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview"
  },
  "dependencies": {
    "firebase": "^9.23.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  }
}
